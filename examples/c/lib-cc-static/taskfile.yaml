version: '3'

vars:
  CACHE_PATH: ".zig-cache"
  OUTPUT_PATH: "zig-out"

tasks:
  clean:
    cmds:
      - rm -rf {{.CACHE_PATH}} {{.OUTPUT_PATH}}

  build:
    dir: "{{.OUTPUT_PATH}}"
    cmds:
      - zig015 cc -o math.o -c ../math.c
      - zig015 ar rcs libmath.a math.o
      - zig015 cc -o lib-cc-static ../main.c -L. -lmath -target native -mcpu=baseline
      - rm -rf libmath.a math.o

  run:
    dir: "{{.OUTPUT_PATH}}"
    cmds:
      - ./lib-cc-static {{.CLI_ARGS}}
